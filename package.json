{
  "scripts": {
    "build": "esbuild llmv.ts llmv.css --bundle --loader:.svg=dataurl --outdir=build",
    "check": "tsc --noEmit llmv.ts"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  }
}